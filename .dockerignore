node_modules
dist
.git
.gitignore
data
*.md
.env
.DS_Store
tsconfig.client.tsbuildinfo
bun-debug.log*

# Carries the registry token.
bunfig.toml

# docker-compose.dev.yml bind-mounts ./db as the Postgres data dir, so it is
# root-owned and the build context sender cannot read it: every `docker build`
# fails with "open db: permission denied" once the dev stack has run.
db

# vendor/ is 1.5 GB, 1.4 GB of it the solver's Rust target/.
vendor
target
.output
.nuxt
.cache
.bun

tests
.github
.devcontainer
.claude
.cursor
.agents
.impeccable
